// contando a quantidade letras do nome
var nome = "Edivan";
console.log(nome.length);
//--------------------------------------------
//Arrendondando o número para inteiro
var altura = 1.79;
(1.79).toFixed();

var btn = document.querySelector(".btn");
btn.addEventListener("click,", function () {});
btn.classList;

console.log(btn);

// Exemplo: É preciso saber que voce a maior parte vai trabalhar com o dom com métodos e propriedades.
// É descobrir métodos e propriedades e saber o que você quer fazer na tela.

// Elemntos QuerySelector etc são chamados de API da web. Que são métodos e propriedade que vai permitir você interagir.

// Métodos são funções, por ela recebe argumento ou não e retoorna um valor.
//EXERCÍCIO ///
// nomeie 5 propriedade ou métodos de elementos do DOM
var btn = document.querySelector(".btn");
// addEventListener
// appendChid
// id
// outerHTML
